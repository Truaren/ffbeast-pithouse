export interface AdcSettings {
    raxis_min: number[];
    raxis_max: number[];
    raxis_smoothing: number[];
    raxis_to_button_low: number[];
    raxis_to_button_high: number[];
    raxis_invert: number[];
}
